<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessMobile.TrainerDetailsPage"
             Title="Trainer Details">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label x:Name="NameLabel" FontSize="24" FontAttributes="Bold" />
            <Label x:Name="SpecLabel" TextColor="Gray" FontSize="18" />
            <Label x:Name="DescLabel" TextColor="Black" />

            <BoxView HeightRequest="1" Color="Gray" />

            <Label Text="Leave a Review" FontAttributes="Bold" />
            <Entry x:Name="CommentEntry" Placeholder="Your opinion..." />
            <Label Text="Rating (1-5):" />
            <Slider x:Name="RatingSlider" Minimum="1" Maximum="5" Value="5" />
            <Button Text="Post Review" Clicked="OnAddReviewClicked" />

            <BoxView HeightRequest="1" Color="Gray" />

            <Label Text="Reviews:" FontAttributes="Bold" FontSize="18"/>
            <CollectionView x:Name="ReviewsList" HeightRequest="300">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout Padding="5">
                            <Label Text="{Binding MemberName}" FontAttributes="Bold" />
                            <Label Text="{Binding Comment}" FontAttributes="Italic" />
                            <Label Text="{Binding Rating, StringFormat='⭐ {0}'}" TextColor="#ffc107"/>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
